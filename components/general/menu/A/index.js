import styled from 'styled-components'
import { rem } from 'polished'

export default styled.a`
  display: block;
  color: #ffffff;
  font-size: ${rem(14)};
  font-family: 'roboto';
  font-weight: 400;
`
