import styled from 'styled-components'
import { rem, em } from 'polished'

const H2 = styled.h1`
  font-size: ${rem(40)};
  font-weight: 700;
  line-height: ${48 / 40};
  margin-bottom: ${em(20, 40)};
`

export default H2
