import { clearFix } from 'polished'
import styled from 'styled-components'

export default styled.div`
  max-width: 1170px;
  position: relative;
  margin-right: auto;
  margin-left: auto;
  ${clearFix()}
`
