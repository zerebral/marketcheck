import styled from 'styled-components'
import { rem } from 'polished'
import { colors } from '%/styles'

const H1 = styled.h1`
  text-align: center;
  color: ${colors.white};
  font-size: ${rem(40)};
  font-weight: 700;
`

export default H1
